info:
  name: register-invalid-combo
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "bad.combo@swaploop.test",
        "password": "password123",
        "displayName": "Bad Combo",
        "batteryMode": "SWAPPABLE",
        "batteryType": "SL-48",
        "connectorType": "GB-AC-48"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A1.4c: POST /register rejects swappable+connector combo with 422", function() {
          expect(res.status).to.equal(422);
          expect(res.body.code).to.equal("VALIDATION_ERROR");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
