meta {
  name: 01 List stations
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/stations
  body: none
  auth: none
}

tests {
  test("returns station collection", function () {
    expect(res.status).to.equal(200);
    expect(res.body.stations).to.be.an("array");
  });
}
