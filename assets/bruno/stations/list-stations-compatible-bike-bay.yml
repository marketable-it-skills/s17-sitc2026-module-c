info:
  name: stations-compatible-bike-bay
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/stations?service=BIKE_BAY&connectorType=GB-AC-48"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A2.4: GET /stations compatible BIKE_BAY GB-AC-48", function() {
          expect(res.status).to.equal(200);
          const ids = res.body.stations.map((s) => s.id).sort();
          expect(ids).to.deep.equal(["station-001", "station-003"]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
