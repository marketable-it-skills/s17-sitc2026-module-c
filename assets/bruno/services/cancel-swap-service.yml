info:
  name: cancel-swap-service
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/services/{{swapReservedServiceId}}/cancel"
  headers:
    - name: Authorization
      value: Bearer sl_tok_rider-001
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("POST /services/{id}/cancel → CANCELLED", function() {
          expect(res.status).to.equal(200);
          expect(res.body.state).to.equal("CANCELLED");
          expect(res.body.completedAt).to.be.a("string");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
