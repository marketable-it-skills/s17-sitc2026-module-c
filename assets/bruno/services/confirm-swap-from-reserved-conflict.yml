info:
  name: confirm-swap-from-reserved-conflict
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/services/{{swapReservedServiceId}}/confirm"
  headers:
    - name: Authorization
      value: Bearer sl_tok_rider-001
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("confirm without start → 409", function() {
          expect(res.status).to.equal(409);
          expect(res.body.code).to.equal("CONFLICT");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
