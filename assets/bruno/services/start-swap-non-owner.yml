info:
  name: start-swap-non-owner
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/services/{{swapServiceId}}/start"
  headers:
    - name: Authorization
      value: Bearer sl_tok_rider-002
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("non-owner start → 403", function() {
          expect(res.status).to.equal(403);
          expect(res.body.code).to.equal("FORBIDDEN");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
