info:
  name: reset
  type: http
  seq: 1

http:
  method: POST
  url: "{{stationServiceBaseUrl}}/reset"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A0.1: Station Service POST /reset restores seed database", function() {
          expect(res.status).to.equal(200);
          expect(res.body.status).to.equal("ok");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
