meta {
  name: 02 Station availability
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/stations/station-001/availability
  body: none
  auth: none
}

tests {
  test("returns seeded station availability", function () {
    expect(res.status).to.equal(200);
    expect(res.body.stationId).to.equal("station-001");
  });
}
