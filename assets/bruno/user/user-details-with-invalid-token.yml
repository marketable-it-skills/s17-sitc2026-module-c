info:
  name: user-details-with-invalid-token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/me"
  headers:
    - name: Authorization
      value: "Bearer sl_tok_rider-999"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A1.7b: GET /me with unknown token returns 401", function() {
          expect(res.status).to.equal(401);
          expect(res.body.code).to.equal("UNAUTHORIZED");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
