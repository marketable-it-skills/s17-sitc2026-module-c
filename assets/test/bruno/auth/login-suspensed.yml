info:
  name: login-suspensed
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: '{ "email": "sun.hao@swaploop.test", "password": "password123" }'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A1.2: POST /login rejects suspended user with 403", function() {
          expect(res.status).to.equal(403);
          expect(res.body.code).to.equal("FORBIDDEN");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
