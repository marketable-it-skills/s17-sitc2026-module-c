info:
  name: user-details-without-token
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/me"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A1.7: GET /me without token returns 401", function() {
          expect(res.status).to.equal(401);
          expect(res.body.code).to.equal("UNAUTHORIZED");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
